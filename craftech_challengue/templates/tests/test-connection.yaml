apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "craftech_challengue.fullname" . }}-test-connection"
  labels:
    {{- include "craftech_challengue.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "craftech_challengue.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
